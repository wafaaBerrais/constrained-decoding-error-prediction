# Normalize line endings for text files
* text=auto
*.sh text eol=lf
*.patch text eol=lf

# Binary artifacts
*.pkl binary
*.pptx binary

# Notebooks only display results: don't count them in GitHub's language stats
*.ipynb linguist-documentation
